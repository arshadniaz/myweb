pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                sh "mvn test ticketbooking"
            }
        }
        stage('package') {
            steps {
                sh "mvn package ticketbooking"
            }
        }
    }
}
